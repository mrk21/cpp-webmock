#include <bandit_with_gmock/bandit_with_gmock.hpp>
#include <webmock/directive/mock.hpp>

namespace webmock { namespace directive {
go_bandit([]{
    using namespace bandit;
    
    describe("webmock::directive::mock", []{
        describe("mock(method, url)", [&]{
            it("should build", [&]{
                
            });
        });
    });
});
}}
